# Load

Generated by `gatekeeper load`. Closed-loop: each worker issues one query, waits,
issues the next — so a slow response delays that worker rather than disappearing
from the histogram. Auditing is **on** except where noted; the audit chain takes a
per-tenant advisory lock and is therefore the component most likely to serialise,
so measuring without it would measure a system nobody runs.

Corpus: 73,801 chunks. Queries drawn from the golden set, principals
round-robin across every asker in it, so load spreads over entitlement buckets of
different selectivity rather than one partial index.

| arm | concurrency | audit | queries | throughput (q/s) | mean | p50 | p95 | p99 | errors |
|:---|---:|:---:|---:|---:|---:|---:|---:|---:|---:|
| full request | 1 | on | 1,671 | 111.3 | 9 ms | 9 ms | 10 ms | 11 ms | 0 |
| full request | 2 | on | 2,351 | 156.6 | 13 ms | 13 ms | 14 ms | 16 ms | 0 |
| full request | 4 | on | 3,158 | 210.4 | 19 ms | 19 ms | 21 ms | 23 ms | 0 |
| full request | 8 | on | 3,052 | 203.3 | 39 ms | 38 ms | 43 ms | 46 ms | 0 |
| full request | 16 | on | 2,905 | 193.4 | 83 ms | 81 ms | 91 ms | 95 ms | 0 |
| full request | 32 | on | 3,015 | 200.4 | 159 ms | 156 ms | 168 ms | 265 ms | 0 |
| full request | 64 | on | 3,030 | 200.7 | 316 ms | 314 ms | 350 ms | 361 ms | 0 |
| full request | 64 | off | 4,383 | 291.3 | 218 ms | 205 ms | 670 ms | 992 ms | 0 |
| query only (no embedding) | 64 | on | 18,158 | 1206.7 | 53 ms | 52 ms | 132 ms | 204 ms | 0 |

## What the numbers say

**Saturation.** Throughput peaks at **210 q/s** at concurrency 4 and stays flat above it while p50 grows roughly linearly (9 ms at concurrency 1 to 314 ms at 64). That is a saturated closed loop: past the knee, added concurrency buys queueing, not work.

**The ceiling is not authorization.** Removing only the in-process embedding pass — same RLS policy, same partial HNSW indexes, same audit chain — raises throughput from 201 q/s to **1207 q/s**, a factor of 6.0. The authorized database path is not what limits this system; the ONNX model sharing a process with the event loop is. The remedy is a separate embedding service, and notably *not* anything that would weaken the access-control design — which is the conclusion the unqualified number invites and the measurement refutes.

**Auditing costs throughput and buys tail latency.** At concurrency 64, turning the audit chain off raises throughput from 201 to 291 q/s (+45%) — and degrades p99 from 361 ms to 992 ms. That is the opposite of the expected trade and it has a mechanism: the chain's per-tenant advisory lock serialises writers, which incidentally paces the whole pipeline. Without it, requests pile onto the connection pool and the tail spreads. The lock is not free, but what it costs is average throughput, not predictability.
